Depuis la racine du projet, exécute ceci :

grep -Rni \
"qualifications.push\|selectedFeature\|selectedExchangeLayer\|capabilityFeatures" \
portal/common/scripts/qualite/standalone/extraction/octane

Puis :

grep -Rni \
"executionSummary\|automatedRuns\|testSuite\|suiteRun" \
portal/common/scripts/qualite/standalone/extraction/octane

Et surtout retrouve le JSON produit par le diagnostic :

find portal -type f -name "*.json" | grep -Ei "octane|diagnostic|qualification"

Envoie-moi la sortie de ces commandes.