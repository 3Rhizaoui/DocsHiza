D’abord, comptons vraiment les qualifications :

node -e "const fs=require('fs'); for(const f of ['portal/commun/data/standalone/octane/qualifications.json','portal/commun/data/standalone/octane/qualifications_brut.json']){const x=JSON.parse(fs.readFileSync(f,'utf8')); console.log('\n'+f); console.log('qualifications=',Array.isArray(x.qualifications)?x.qualifications.length:'absent'); if(Array.isArray(x.qualifications)){x.qualifications.forEach((q,i)=>console.log(i+1, q.featureName||q.name||q.capabilityName||q.feature?.name||'SANS NOM'));}}"

Ensuite affiche le bloc de sélection :

sed -n '880,1065p' \
portal/commun/scripts/qualite/standalone/extraction/octane/diagnostiquer_hierarchie_octane.js

Puis affiche la partie où les données finales sont construites :

sed -n '1220,1320p' \
portal/commun/scripts/qualite/standalone/extraction/octane/diagnostiquer_hierarchie_octane.js