Sur le PC BNP, exécute ces 3 commandes et envoie-moi le résultat.

echo "===== ZONE 9190-9370 ====="
sed -n '9190,9370p' \
portal/reporting/general/index.html

Puis :

echo "===== ZONE 9635-9790 ====="
sed -n '9635,9790p' \
portal/reporting/general/index.html

Puis :

echo "===== SPRINT LABEL ====="
grep -n -A8 -B8 \
'sprintLabel' \
portal/reporting/general/index.html

Et si possible aussi ce petit diagnostic JSON :

python - <<'PY'
import json
from pathlib import Path

p = Path(
    "portal/commun/data/jira/presentation/"
    "payload_dashboard_final.json"
)

d = json.loads(
    p.read_text(
        encoding="utf-8",
        errors="replace"
    )
)

print("sprintCourant =", d.get("sprintCourant"))

for key in (
    "sprintJiraSynthese",
    "contributionSprintCourant",
    "sprintJiraCourant",
):
    x = d.get(key) or {}
    print()
    print(key)
    print("sprint =", x.get("sprint"))
    print("nom =", x.get("nom"))
    print("label =", x.get("label"))
    print("totalJira =", x.get("totalJira"))
PY